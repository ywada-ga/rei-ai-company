# REI 実装状況

更新: 2026-09-25

## 完成の判定

一般ユーザーに配布できる「完成」は、次をすべて満たした状態とする。

1. 初回セットアップ、所有者登録、端末追加、仕事の依頼・承認・中止・結果確認、プロジェクト管理がmacOSとWindowsの実機で通る。
2. 複数PCが安全な遠隔経路で接続し、切断・再起動後も復帰できる。端末が実行中に切れた場合も、二重実行を避けて状態が分かる。
3. Chatworkの人への依頼と返答照合を実アカウントで確認し、MCPの主要サービスは認証・ツール検出・実行まで確認する。
4. 権限境界、秘密情報の保管、バックアップ・復元、運用ログ、インストール・更新手順を検証する。
5. 利用者向けの導入手順と制約を整え、Mac・Windows・LinuxのCIと代表的な実機操作が通る。

このPCのChatGPTデスクトップアプリを直接操作する連携は未実装。OpenClaw経由で仕事を実行する現方式との役割整理と、配布可能な接続方式の検証が必要。

## 動作を確認したこと

- Node.jsの構文検査
- 別の一時データベースで、所有者登録 → ログイン → 端末登録 → 計画 → 実行結果 → 稼働報告
- このPCのOpenClaw `main` を実際に呼び出し、計画と実行の結果が親仕事へ戻るまで
- このPCでHubとConnectorをログイン時に自動起動し、常駐状態で依頼の完了結果が戻るまで
- パスワード変更後の再ログイン
- 10分間の接続コードによる端末登録と、コードの再利用拒否
- Windows向けのOpenClaw CLI起動とログイン時の自動起動を実装。Windows・macOS・LinuxのCIでREI本体を検証し、Windowsでは引用符や改行を含む指示文が模擬CLIにそのまま届くことを確認
- ローカルWeb画面の初期登録表示、ブラウザのエラーログなし
- MCP連携の登録・対象PCへの配信・認証待ち状態・解除を一時DBと模擬OpenClawで確認。Notion、Linear、Atlassian、Googleカレンダー・Gmail・ドライブの公式URLを選べる画面も確認
- このPCのOpenClaw CLIで実際にMCPを登録し、状態を読み、解除する操作を確認。実アカウントへのOAuth接続は未実施
- 国内向けMCPを含むチェックボックス式の一括登録をブラウザで確認。kintone公式ドキュメントMCPは、このPCへ登録して3ツールの公開を実際に確認し、検証後に解除
- Web・Xで挙がる候補を調べ、公式URLと接続条件を確認したMCPを追加。候補の選定と非対応の理由はMCP_TREND_CATALOG.mdに記録
- 連携先ごとの接続確認を追加。対象PCでOpenClawのMCP検査を行い、認証待ち・成功時のツール数・失敗理由をHubへ返す。kintone公式ドキュメントMCPで画面上の「接続成功・3ツール」まで実機確認し、検証後に解除
- プロジェクトの作成・状態変更・仕事との関連付け、進捗集計、権限確認を一時DBの結合テストで確認
- ミッション詳細の工程・履歴取得と、実行前の中止を結合テストで確認。作業中の仕事は中止不可
- 稼働中のSQLiteからバックアップを作成し、SHA-256・SQLite整合性検証、新しいフォルダへの復元、改ざん検知をテストで確認
- Tailscaleの導入・ログイン・Serve状態を検出し、所有者の操作で安全な入口を有効化して接続URLを自動入力する導線を追加
- Chatwork未設定時にも人への回答を画面から記録・集約できるようにし、送信処理の重複取得を防止
- 所有者による利用者の権限変更、利用停止・再開、停止時のログイン失効を一時DBの結合テストで確認
- このPCの既存OpenClaw `main` をREI用の表示名・作業場所・指示に変更し、計画・実行の通しテストを確認。変更前のOpenClaw設定はローカルに退避。新しい参加PCでは端末追加時に専用エージェントを作成し、直接メッセージ送信とGateway管理ツールを無効化する処理を実装
- 計画担当PCがオフラインのとき、計画機能のある別PCが引き継いで計画・実行を完了する流れを結合テストで確認
- 通信断で「要確認」になった工程に、所有者・管理者が確認結果と根拠を記録し、親仕事を再集計する操作を結合テストで確認
- 計画担当の失敗で「要確認」になった仕事を、所有者・管理者が計画だけ再実行して完了まで進める操作を結合テストで確認
- 計画結果がJSONでない場合、元の指示を勝手に実行せず「要確認」にして、計画の再実行を待つ動作を結合テストで確認
- 停止中のPCに割り当てられた未着手の工程を、接続中の別PCへ移して完了させる流れを結合テストで確認。開始済み工程の移動は拒否
- Chatwork送信中にHubが停止した場合、再起動後に「要確認」へ移し、二重投稿しない動作をテストで確認
- 日次報告にPCごとの計画・実行工程数と人への依頼件数を追加。REIに登録した仕事だけを日本時間で集計し、実画面で表示を確認
- OpenClaw CLIの結果状態・中断状態・失敗したツール結果を完了扱いにしないよう修正し、このPCのOpenClawとの通しテストを再実行
- 計画が存在しないPCを指定したり13工程以上になった場合、別PCへ黙って割り当てたり工程を省略せず「要確認」にする検証を追加
- 2台のConnectorが同時に独立した工程を取得し、同じ工程を二重に取得せず、両方の結果を親仕事へ集約する結合テストを追加
- ミッション履歴を100件ずつ追加で読み込めるようにし、一覧では長い結果を省略、詳細では全文を維持。100件超・同時刻の仕事でも重複や抜けがないことを結合テストと別の検証用ブラウザで確認
- Linux向けにsystemdユーザーサービスの自動起動設定を追加し、CI上で生成内容を確認
- Mac・Windows・Linux別の更新・再起動・コードのロールバックと、データを上書きしない復元手順を追加
- 結果保存後に応答が失われた場合の再送を重複なしで受理。実行工程が通信断で「要確認」になった後に届く結果は証拠として記録し、勝手に完了にはしないことを結合テストで確認
- Linuxのかんたん端末追加でも、macOS・Windowsと同様にConnectorの自動起動登録まで進むよう変更
- ログイン期限切れ時に以前の仕事・端末表示を消してログイン画面へ戻し、Hub稼働中でも端末がいない状態を「NO AGENT」と区別。画面へ戻った直後に端末状態を更新
- 「かんたん端末追加」で登録された最初のPCを計画担当にし、接続コマンドへURLを含めて入力を接続コードだけに削減
- 中心PC自身を接続する専用の導線を設定画面に追加。既存のConnector設定を検出し、正常・停止中・要確認を区別して不用意な上書きを防止
- 端末の参加コマンドも既存のConnector設定があれば登録前に停止し、設定ファイルを上書きしないことをテストで確認
- 復元先などで `REI_DATA_DIR` を変えた場合、HubとConnectorが同じ接続設定を参照するよう修正。Mac・Windows・Linuxの自動起動にも保存先とポートの設定を引き継ぐ
- バックアップ時点で保存されているConnectorの送信待ち結果も復元対象へ追加
- Tailscale Serveの接続確認と有効化で、REIを標準以外のポートにした場合も実際のポートを使うよう修正
- macOSの自動起動設定を隔離した試験先へ生成し、復元データの保存先・接続設定・ポート・ファイル権限を確認

## 実機で接続するために必要な情報

1. Mac mini 3台で使うTailscaleの参加設定と、それぞれのOpenClawの利用可能なエージェント名。
2. ChatworkのルームIDとAPIトークン。これはREIの設定画面に所有者が入力できる。
3. Codex Pulseのリポジトリまたは公開URL。画面設計の参考先を特定するため。

## 配布前に追加で検証すること

- Mac mini 3台でのTailscale Serve接続、各端末でのOpenClaw実行。手動SSH方式を使う場合は転送の継続・再接続も確認。
- Chatworkの実送信、API制限、返信照合。
- Google Workspace Developer Previewの利用資格とOAuthクライアントの準備後、カレンダー・Gmailの実認証とツール実行。一般的なMCPの接続確認も必要。
- 高権限のOpenClawツールをREI専用エージェントでどこまで許可するか。
- Linux実機での自動起動と、一般利用者向けの配布用インストーラー。
- 同時実行、長文、OS再起動、バックアップの長期運用と外部ストレージへの保管の確認。
