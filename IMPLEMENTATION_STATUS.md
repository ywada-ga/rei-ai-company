# REI 実装状況

更新: 2026-09-26

## 完成の判定

一般ユーザーに配布できる「完成」は、次をすべて満たした状態とする。

1. 初回セットアップ、所有者登録、端末追加、仕事の依頼・承認・中止・結果確認、プロジェクト管理がmacOSとWindowsの実機で通る。
2. 複数PCが安全な遠隔経路で接続し、切断・再起動後も復帰できる。端末が実行中に切れた場合も、二重実行を避けて状態が分かる。
3. Chatworkの人への依頼と返答照合を実アカウントで確認し、MCPの主要サービスは認証・ツール検出・実行まで確認する。
4. 権限境界、秘密情報の保管、バックアップ・復元、運用ログ、インストール・更新手順を検証する。
5. 利用者向けの導入手順と制約を整え、Mac・Windows・LinuxのCIと代表的な実機操作が通る。

このPCのChatGPTデスクトップアプリを直接操作する連携は未実装。OpenClaw経由で仕事を実行する現方式との役割整理と、配布可能な接続方式の検証が必要。

## 動作を確認したこと

- Node.jsの構文検査
- 別の一時データベースで、所有者登録 → ログイン → 端末登録 → 計画 → 実行結果 → 稼働報告
- このPCのOpenClaw `main` を実際に呼び出し、計画と実行の結果が親仕事へ戻るまで
- このPCでHubとConnectorをログイン時に自動起動し、常駐状態で依頼の完了結果が戻るまで
- パスワード変更後の再ログイン
- 10分間の接続コードによる端末登録と、コードの再利用拒否
- Windows向けのOpenClaw CLI起動とログイン時の自動起動を実装。Windows・macOS・LinuxのCIでREI本体を検証し、Windowsでは引用符や改行を含む指示文が模擬CLIにそのまま届くことを確認
- ローカルWeb画面の初期登録表示、ブラウザのエラーログなし
- MCP連携の登録・対象PCへの配信・認証待ち状態・解除を一時DBと模擬OpenClawで確認。Notion、Linear、Atlassian、Googleカレンダー・Gmail・ドライブの公式URLを選べる画面も確認
- このPCのOpenClaw CLIで実際にMCPを登録し、状態を読み、解除する操作を確認。実アカウントへのOAuth接続は未実施
- 国内向けMCPを含むチェックボックス式の一括登録をブラウザで確認。kintone公式ドキュメントMCPは、このPCへ登録して3ツールの公開を実際に確認し、検証後に解除
- Web・Xで挙がる候補を調べ、公式URLと接続条件を確認したMCPを追加。候補の選定と非対応の理由はMCP_TREND_CATALOG.mdに記録
- 連携先ごとの接続確認を追加。対象PCでOpenClawのMCP検査を行い、認証待ち・成功時のツール数・失敗理由をHubへ返す。kintone公式ドキュメントMCPで画面上の「接続成功・3ツール」まで実機確認し、検証後に解除
- プロジェクトの作成・状態変更・仕事との関連付け、進捗集計、権限確認を一時DBの結合テストで確認
- ミッション詳細の工程・履歴取得と、実行前の中止を結合テストで確認。作業中の仕事は中止不可
- 稼働中のSQLiteからバックアップを作成し、SHA-256・SQLite整合性検証、新しいフォルダへの復元、改ざん検知をテストで確認
- Tailscaleの導入・ログイン・Serve状態を検出し、所有者の操作で安全な入口を有効化して接続URLを自動入力する導線を追加
- Chatwork未設定時にも人への回答を画面から記録・集約できるようにし、送信処理の重複取得を防止
- 所有者による利用者の権限変更、利用停止・再開、停止時のログイン失効を一時DBの結合テストで確認
- このPCの既存OpenClaw `main` をREI用の表示名・作業場所・指示に変更し、計画・実行の通しテストを確認。変更前のOpenClaw設定はローカルに退避。新しい参加PCでは端末追加時に専用エージェントを作成し、直接メッセージ送信とGateway管理ツールを無効化する処理を実装
- 計画担当PCがオフラインのとき、計画機能のある別PCが引き継いで計画・実行を完了する流れを結合テストで確認
- 通信断で「要確認」になった工程に、所有者・管理者が確認結果と根拠を記録し、親仕事を再集計する操作を結合テストで確認
- 計画担当の失敗で「要確認」になった仕事を、所有者・管理者が計画だけ再実行して完了まで進める操作を結合テストで確認
- 計画結果がJSONでない場合、元の指示を勝手に実行せず「要確認」にして、計画の再実行を待つ動作を結合テストで確認
- 停止中のPCに割り当てられた未着手の工程を、接続中の別PCへ移して完了させる流れを結合テストで確認。開始済み工程の移動は拒否
- Chatwork送信中にHubが停止した場合、再起動後に「要確認」へ移し、二重投稿しない動作をテストで確認
- 日次報告にPCごとの計画・実行工程数と人への依頼件数を追加。REIに登録した仕事だけを日本時間で集計し、実画面で表示を確認
- OpenClaw CLIの結果状態・中断状態・失敗したツール結果を完了扱いにしないよう修正し、このPCのOpenClawとの通しテストを再実行
- 計画が存在しないPCを指定したり13工程以上になった場合、別PCへ黙って割り当てたり工程を省略せず「要確認」にする検証を追加
- 2台のConnectorが同時に独立した工程を取得し、同じ工程を二重に取得せず、両方の結果を親仕事へ集約する結合テストを追加
- ミッション履歴を100件ずつ追加で読み込めるようにし、一覧では長い結果を省略、詳細では全文を維持。100件超・同時刻の仕事でも重複や抜けがないことを結合テストと別の検証用ブラウザで確認
- Linux向けにsystemdユーザーサービスの自動起動設定を追加し、CI上で生成内容を確認
- Mac・Windows・Linux別の更新・再起動・コードのロールバックと、データを上書きしない復元手順を追加
- 結果保存後に応答が失われた場合の再送を重複なしで受理。実行工程が通信断で「要確認」になった後に届く結果は証拠として記録し、勝手に完了にはしないことを結合テストで確認
- Linuxのかんたん端末追加でも、macOS・Windowsと同様にConnectorの自動起動登録まで進むよう変更
- ログイン期限切れ時に以前の仕事・端末表示を消してログイン画面へ戻し、Hub稼働中でも端末がいない状態を「NO AGENT」と区別。画面へ戻った直後に端末状態を更新
- 「かんたん端末追加」で登録された最初のPCを計画担当にし、接続コマンドへURLを含めて入力を接続コードだけに削減
- 中心PC自身を接続する専用の導線を設定画面に追加。既存のConnector設定を検出し、正常・停止中・要確認を区別して不用意な上書きを防止
- 端末の参加コマンドも既存のConnector設定があれば登録前に停止し、設定ファイルを上書きしないことをテストで確認
- 復元先などで `REI_DATA_DIR` を変えた場合、HubとConnectorが同じ接続設定を参照するよう修正。Mac・Windows・Linuxの自動起動にも保存先とポートの設定を引き継ぐ
- バックアップ時点で保存されているConnectorの送信待ち結果も復元対象へ追加
- Tailscale Serveの接続確認と有効化で、REIを標準以外のポートにした場合も実際のポートを使うよう修正
- macOSの自動起動設定を隔離した試験先へ生成し、復元データの保存先・接続設定・ポート・ファイル権限を確認
- macOSのかんたん端末追加を隔離したOpenClaw CLIとHubで通し、専用エージェント・接続設定・自動起動登録まで確認。稼働中のREIは変更していない
- 計画担当AIへ端末ごとのエージェント名と現在の接続状態を渡し、担当PCを選ぶ材料を追加。心拍にAI名が省略されても保存済みの名前を維持し、このPCのOpenClawで計画から実行まで再確認
- OpenClawで長い仕事を実行中もConnectorが心拍を送り、端末が誤ってオフライン表示にならないことを遅延する模擬CLIで確認
- Hubが結果の照合を拒否した場合、Connectorの送信待ち結果を削除せず、次の仕事も取得しないことを検証。Hubが受理した再送だけ記録を消す
- 参加PCに送信待ち結果が残った件数を心拍でHubへ伝え、司令室と端末設定に表示する。Hubが受理した後に件数を消す挙動を結合テストで確認
- 再起動時に送信待ちファイルが壊れていたり保存途中の一時ファイルが残っていたりする場合、空の記録として進めず停止する。保存失敗時にも仕事を続けないよう変更
- 実行中の端末を解除した場合、計画の再試行と実行工程の「要確認」を期限切れまで待たず直ちに反映する流れを結合テストで確認
- Tailscale Serveの設定をJSONのHTTPSルートと転送先で判定し、別パスや公開用Funnelを安全な接続として案内しないことを模擬CLIで確認。別サービスが入口を使用中なら自動上書きを止める
- 別ポートの隔離したREIをブラウザで初期登録・ログインし、送信待ち2件の警告が司令室、接続ユニット、端末設定に表示されることを確認。稼働中のデータは変更していない
- Chatwork自身のアカウントIDを設定時に保存し、既存設定では初回取得時に補完。自分の投稿・投稿者不明の投稿を人の回答から除外し、投稿IDが不明な送信を「要確認」にする模擬APIテストを追加
- バックアップに送信待ち結果の保存途中ファイルも含め、復元後も照合前の証拠が残ることを隔離したデータで確認
- バックアップ先・復元先のシンボリックリンクを拒否し、別フォルダへの意図しない書き込みを隔離テストで確認
- MCP設定の記録が壊れた場合や保存途中ファイルが残った場合は追加・解除を止め、バックアップには保存途中ファイルも含めることを隔離テストで確認
- Chatworkの回答に異なる仕事IDが複数含まれる場合は、どの仕事も完了扱いにしないことを模擬APIで確認
- 前日に始めて今日完了した仕事、PC工程、人の回答も今日の稼働報告に含むことを日付境界のテストで確認
- 稼働報告の一覧を最新の活動順に並べ、前日開始・今日完了した仕事には今日の完了時刻を表示する
- 依頼者を閲覧者へ変更した後、以前に作った仕事の中止ボタンと中止APIが使えなくなることを結合テストで確認
- 参加PCは接続コードの有効性をHubで先に確認し、無効なコードならOpenClawの設定を変更しない
- ログアウトと期限切れ時に稼働報告・履歴などの表示用データを消し、報告画面は再入場時と表示中に更新する
- 端末追加画面は現在確認できるTailscale ServeのURLだけで遠隔接続コードを発行し、接続が切れたら古いURLを消す
- 手動のConnector設定でも既存の接続ファイルを上書きせず、再設定前に既存情報の確認を求める
- 保留中のプロジェクトを選択したままでも「今日の稼働報告」を開けるようにした
- Hubと参加PCのREI版を表示し、古い版のまま接続しているPCを司令室と端末設定で知らせる
- バックアップ対象の設定・鍵・送信待ち結果が通常のファイルでない場合は、欠けたバックアップを作らず停止する
- 端末追加がOpenClawエージェント作成後に中断されても、再実行で不足したREI用ファイルと識別設定を補完する
- 参加PCの診断コマンドでNode.js、OpenClaw、接続設定、Hub到達、端末トークン、送信待ち結果を確認し、秘密情報を表示しない
- 診断でHubと参加PCの版違い、未送信の結果、壊れた送信待ち記録も「要確認」にする
- 今日の稼働報告に前日以前から残る「要確認」を別枠で表示し、古い仕事も詳細へ直接開けるようにする
- 公開GitHubリポジトリを別フォルダへ新規取得し、追跡ファイルにデータベース・認証情報・環境設定が含まれないことを確認。macOSで構文検査を通し、空の別データ保存先と別ポートでHubを起動して初回登録待ちのAPI応答と画面配信を確認
- 設定画面で権限のない利用者に承認・端末解除・端末追加・Chatwork設定の操作を表示せず、権限のないTailscale状態確認も呼ばないようにする
- このPCで稼働中のREIデータをバックアップし、目録・ハッシュ・SQLite整合性を検証。別の空フォルダへの復元も確認し、検証用コピーは削除。バックアップ本体はGit管理外の `data/backups/` に保持
- macOS・Windows・Linux共通のHub自動起動コマンドを追加。OS別インストーラーへ引き継ぎ、初回登録後に使う順序を導入手順に明記
- 所有者未登録のHubを通常URLで開いたとき、ログイン画面に初回登録URLの場所を案内する
- Tailscaleの接続有効化は所有者だけにボタンを表示し、管理者には所有者へ依頼する案内を表示する
- REI版がHubと異なる端末の新しい仕事の取得を止め、処理済み結果の返送は維持する。計画を取得後に版違いとなった端末が結果を返し、次の実行工程だけ止まる流れを結合テストで確認
- 稼働報告を日本語の文章にしてコピーする操作を追加。Chatwork未設定でも手動共有でき、隔離したHubとブラウザでコピー成功表示を確認
- 版違い時の仕事取得制御を反映するため、REIの版を0.5.0へ更新。テストは現在の版をパッケージから読み、将来の更新でも版番号の整合性を確認
- 版番号を送らない旧Connectorも新しい仕事の取得を停止。結果返送は維持し、旧計画担当をオンラインの引継ぎ候補と誤認しない
- バックアップ元のSQLiteと検証対象のSQLiteがシンボリックリンクの場合は拒否し、意図しない別ファイルを保存・復元しないことを隔離テストで確認
- 接続切れの工程・親仕事・履歴を一つの保存処理で確定。履歴の保存失敗を再現し、途中まで状態が変わらないことを確認
- 中心PC用の診断で初回登録、SQLite整合性、Hub応答、最新バックアップを確認。欠けた・壊れたバックアップを隔離テストで検出
- 端末がオンラインでも版違いで仕事を受け取れない場合、司令室を準備完了と表示しないようにした
- 新しく準備するREI用OpenClawエージェントは、ツール権限の既定を明示的な `coding` プロファイルに固定。既存の明示設定は保持し、メッセージ送信・Gateway管理の禁止を維持
- `REI_DATA_DIR` を指定した新規OpenClawエージェントの作業場所と設定退避先を同じ保存先へ揃え、既存の作業場所は維持する
- REI用作業場所と指示ファイルにリンクや通常ファイル以外がある場合は設定を止め、指示文が別ファイルに書き込まれないことを隔離テストで確認
- バックアップを一時フォルダで作成・検証してから完成名へ移動し、失敗した未完成データを最新バックアップとして扱わないようにした
- 復元も別フォルダでコピーとSQLite整合性検査を済ませてから新しい復元先へ確定し、失敗時に一部だけ展開した保存先を残さない
- 参加PCの診断でREI担当AIの直接送信・Gateway管理の禁止設定を読み取り、設定が外れた場合に警告する。このPCのOpenClawでも正常を確認
- 未着手の仕事を別PCへ移す際、接続中でもREIの版が異なり仕事を受け取れない端末を選択肢とAPIの両方で除外する
- 端末が報告した計画・実行機能に合う工程だけを取得できるようにし、機能がないPCへの担当変更も拒否。司令室の準備状態も両機能の有無で判定
- Chatworkの返信履歴・仕事の完了・親仕事の更新を一括保存。途中で履歴の保存に失敗した場合は回答を未処理のまま残し、次回取得で再試行できることを模擬APIで確認
- Tailscale Serveの443番が別サービスや別パスに使われている場合、REIの入口を自動設定せず競合として知らせる。模擬設定で既存の入口を上書きしないことを確認
- 端末の接続コード使用、端末登録、履歴記録を一括保存。履歴保存を失敗させてもコードが使えるままで、再試行できることを結合テストで確認
- macOSの自動起動設定更新に失敗した場合、以前の設定へ戻して再起動。新規登録に失敗した場合は設定ファイルを残さないことを隔離テストで確認
- Linuxの自動起動設定更新で既存サービスを再起動して新設定を反映。起動失敗時は旧設定へ戻し、新規登録失敗時は不完全なサービスを解除する模擬systemdテストを追加
- 端末追加画面にコマンドと接続コードのコピー操作を追加。初回登録からログイン、このPC用・別PC用コード発行、コピー完了表示まで隔離したHubと実画面で確認
- 手動端末登録とMCPの追加・解除を履歴記録と一括保存。履歴保存を失敗させた場合に登録や解除が途中まで反映されず、再試行できることを結合テストで確認
- パスワード・利用者の権限・プロジェクトの作成と状態変更を履歴記録と一括保存。履歴保存失敗時にパスワード、権限、プロジェクト状態が変わらないことを結合テストで確認
- 通信断後に届いた実行結果の照合情報を保存。人が確認結果を記録した後に端末が同じ報告を再送しても受理し、異なる報告は拒否する流れを結合テストで確認
- 人の確認が先に完了し、その後に端末の結果が届いた場合も報告を保存し、仕事を再び要確認にする。元の確認内容と遅れた報告の両方を詳細に残し、再確認で完了できることを結合テストで確認
- Google公式資料で接続先を確認し、People API（連絡先・社内ディレクトリ）とWorkspace横断検索のMCPをチェック式の候補へ追加。どちらも開発者向けプレビューとOAuth準備が必要なことを画面に表示
- Chatworkの送信開始・投稿ID保存をそれぞれ仕事の状態と履歴と一括保存。送信前の履歴失敗時は投稿せず再試行でき、投稿後の保存失敗時は二重送信せず要確認になることを模擬APIで確認
- Chatwork暗号鍵の欠損・不正なファイルを読取時に検出し、勝手に新しい鍵を作らず設定画面へ要確認を表示。鍵を復元すれば元の設定で再接続できることを隔離テストで確認
- 画面のHTML・JavaScript・CSSを保存せず配信し、REI更新後に古い画面がブラウザへ残らないようにした
- Chatworkの設定画面に公式のルームID・APIトークン取得手順と、組織で必要になる管理者承認の案内を追加
- 中心PCの診断で最新バックアップが7日以上古い場合に要確認とし、保存忘れを知らせる
- OpenClawの工程期限を固定3分から既定30分に延長し、60〜7200秒で調整できるようにした。期限を過ぎても残るプロセスを停止させ、実行中の心拍と仕事期限更新は維持する
- 工程期限の設定を参加PCの診断で検査し、無効な値ではMac・Windows・Linuxの自動起動登録を始めないようにした
- SQLiteのデータ形式に版を付け、将来の形式を古いREIが開いた場合はデータを変更せず起動を止める。既存の版なしデータを更新して記録を維持することも隔離テストで確認
- 参加PCの診断でOpenClaw担当AIの既定モデルと不足している認証プロバイダーを確認。このPCではOpenAIのモデル設定があり、認証不足は表示されなかった。実通信の成功は既存の通しテストで確認済み
- 仕事の履歴が300件を超えても詳細画面に最新記録が出るよう取得順を修正。多数の履歴を加える結合テストで新しい記録の表示を確認
- 仕事ごとの履歴を画面から段階的に読み込めるようにし、300件を超えた過去の記録にも到達可能にした。450件の記録でページ間の重複・欠落を結合テストで確認し、隔離した実画面でも451件目まで表示・画面エラーなしを確認
- Windowsの自動起動設定を一時ファイルで完成させてから入れ替え、起動を開始できない場合は以前の設定へ戻すようにした。設定更新と不正な設定を拒否した後の状態をWindows CIで確認した
- 端末が長い日本語の結果を送ると、受信の分割位置で文字が壊れたり20万文字付近で拒否されたりする問題を修正。認証済み端末の結果だけ最大8MBまで受け取り、約20万文字の全文保存を結合テストで確認

## 実機で接続するために必要な情報

- 中心PCではTailscaleの公式Standalone版が未導入。パッケージ取得までは確認したが、macOSの管理者認証が必要で自動インストールは止まった。REIの端末設定画面に公式手順と必要な操作を表示した。

1. Mac mini 3台で使うTailscaleの参加設定と、それぞれのOpenClawの利用可能なエージェント名。
2. ChatworkのルームIDとAPIトークン。これはREIの設定画面に所有者が入力できる。
3. Codex Pulseのリポジトリまたは公開URL。画面設計の参考先を特定するため。

## 配布前に追加で検証すること

- Mac mini 3台でのTailscale Serve接続、各端末でのOpenClaw実行。手動SSH方式を使う場合は転送の継続・再接続も確認。
- Chatworkの実送信、API制限、返信照合。
- Google Workspace Developer Previewの利用資格とOAuthクライアントの準備後、カレンダー・Gmailの実認証とツール実行。一般的なMCPの接続確認も必要。
- 高権限のOpenClawツールをREI専用エージェントでどこまで許可するか。
- Linux実機での自動起動と、一般利用者向けの配布用インストーラー。
- 同時実行、長文、OS再起動、バックアップの長期運用と外部ストレージへの保管の確認。
